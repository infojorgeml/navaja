---
// src/components/Container.astro — wrapper centrado (boxed, 1400px)
interface Props {
  class?: string;
}
const { class: cls = '' } = Astro.props;
---

<div class:list={['boxed', cls]}>
  <slot />
</div>
